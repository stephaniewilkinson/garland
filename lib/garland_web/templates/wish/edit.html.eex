<h1>Edit Wish</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_wish_path(@conn, :update, @wish, @user)) %>

<span><%= link "Back", to: Routes.user_wish_path(@conn, :index, @user) %></span>
